import http from 'node:http';import fs from 'node:fs';import path from 'node:path';const port=Number(process.env.PORT||5173);http.createServer((req,res)=>{let p=decodeURI(req.url.split('?')[0]);p=p.replace(/^\/IWT-Open-Sanctions/,'');if(p==='/'||!p)p='/index.html';const file=path.join(process.cwd(),p);if(!file.startsWith(process.cwd())){res.writeHead(403);return res.end()}fs.readFile(file,(e,b)=>{if(e){res.writeHead(404);res.end('Not found')}else{res.setHeader('Content-Type',p.endsWith('.js')?'text/javascript':p.endsWith('.css')?'text/css':p.endsWith('.json')?'application/json':p.endsWith('.html')?'text/html':'application/octet-stream');res.end(b)}})}).listen(port,()=>console.log(`http://localhost:${port}/IWT-Open-Sanctions/`));
